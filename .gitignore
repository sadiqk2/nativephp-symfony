# Generated demo application — recreate it with spike/README.md
/spike/app/

# Upstream reference clones — shallow, read-only, cloned on demand
/upstream/

# Composer / npm
/bundle/vendor/
/bundle/.phpunit.cache/
node_modules/

# The docs build installs marked and highlight.js ad hoc (tools/README.md), which
# scaffolds a package.json at the root. This is not a JavaScript project and does not
# own one.
/package.json
/package-lock.json

# Build output
dist/
/mobile-bundle/vendor/
/mobile-bundle/.phpunit.cache/

# Demo app — generated. `demo/README.md` reconstructs it.
/demo/vendor/
/demo/var/
/demo/nativephp/
