[{"t":"Home","u":"index.html","b":"Symfony as a desktop, iOS and Android application","h":[],"x":"NativePHP for Symfony desktop iOS Android documentation getting started"},{"t":"Introduction","u":"introduction.html","b":"What is here, what is authoritative, and what has drifted","h":[{"t":"Using it","a":"using-it"},{"t":"A worked example","a":"a-worked-example"},{"t":"Reference — the protocol, not the guide","a":"reference--the-protocol-not-the-guide"},{"t":"Understanding it","a":"understanding-it"},{"t":"Working on the port","a":"working-on-the-port"},{"t":"Historical record","a":"historical-record"},{"t":"Known drift in older documents","a":"known-drift-in-older-documents"}],"x":"Documentation Start here. This directory is the documentation for using NativePHP for Symfony; the Markdown files at the repository root are mostly analysis, specification, or a record of how the port was built. Using it --- --- Getting started — desktop to a window on screen, including the two things that are not automatic Getting started — mobile Both render paths, and what \"verified by tests, not by a device\" means Desktop API reference Windows, menus, dialogs, notifications, clipboard, settings, child processes, Messenger, events Mobile API reference The 16 bridge groups, and the native-UI element/component/style layers Recipes Multi-window apps, a Messenger worker, typed listeners, a stateful native screen, packaging Testing , , , Troubleshooting Symptom → cause → fix. Read this first when something is silently wrong, which is this runtime's usual failure mode A worked example is a Symfony 8 application using both bundles: a bootstrapper, a multi-window inspector, a notes CRUD over the settings store, a Messenger-style job page, a menu with routed events, a native-UI counter screen, and a Twig function the app owns rather than the bundle. Read it after the getting-started page for this project's idea of idiomatic use. It is newer than these docs; where it disagrees, it is probably right. Reference — the protocol, not the guide Current and authoritative. Consult these when a wrapper does not cover what you need, or when you are checking a payload shape. --- --- The desktop wire protocol: all 116 endpoints with request and response shapes, all 44 events, the environment contract, and the seven things the runtime requires of any PHP app The mobile native-UI wire format: node shapes, content hashes, id derivation, the callback protocol, the style vocabulary Desktop package overview Mobile package overview The two package READMEs are summaries and lag the code slightly — see known drift below. Where they disagree with these docs, these docs were written against the "},{"t":"Desktop","u":"desktop.html","b":"composer require to a window on screen","h":[{"t":"1. Install the bundle","a":"1-install-the-bundle"},{"t":"2. Import the bundle's routes","a":"2-import-the-bundles-routes"},{"t":"3. Implement AppBootstrapper","a":"3-implement-appbootstrapper"},{"t":"4. Install the Electron runtime into your project","a":"4-install-the-electron-runtime-into-your-project"},{"t":"5. Run it","a":"5-run-it"},{"t":"6. Two commands that must print nothing but JSON","a":"6-two-commands-that-must-print-nothing-but-json"},{"t":"7. Optional: write a manifest","a":"7-optional-write-a-manifest"},{"t":"8. Configuration","a":"8-configuration"},{"t":"9. Where to go next","a":"9-where-to-go-next"}],"x":"Getting started — desktop From to a Symfony application in a native window. You need PHP 8.3+, Symfony 7 or 8, Node 20+ (the runtime is an Electron project and is built with npm), and a working . Nothing about your application changes: it is an ordinary Symfony app served by PHP's built-in server, and Electron points a at it. --- 1. Install the bundle Not on Packagist yet , so that resolves only once Composer knows where to find it: clone this repository and add a path repository pointing at , with (a copy goes stale and edits to the bundle appear to do nothing). The exact block is in the root README, and is a working example. Everything after this step is unaffected by how the package arrived. Flex registers the bundle for you: it derives candidate class names from the PSR-4 namespace, and yields . That is what the namespace is shaped for — the bundle used to be , where the candidates are and and the real class matched neither, so every application had to register it by hand. Without Flex, add it yourself: 2. Import the bundle's routes (step 4) writes this file for you, so you can skip ahead — it is documented here because it is worth knowing what it does and why, and because an application that is not Flex-shaped has to write it by hand. Symfony bundles cannot register routes on their own. The runtime posts to two fixed paths, and , and until they are imported the app boots into a window that never opens and receives no events: The installer only ever creates this file; it will not rewrite one you have edited unless you pass , and if there is no directory it warns rather than writing somewhere nothing loads. Verify with — you want and . 3. Implement The runtime finishes its own boot and then POSTs . Whatever that handler does is your application's startup. Nothing appears on screen unless you ask for it: You do not wire anything. The bundle calls on and a compiler pass aliases the interface to your single implementation. If you register two, the pass fails at comp"},{"t":"Mobile","u":"mobile.html","b":"iOS and Android, and what a build needs","h":[{"t":"The one architectural difference from desktop","a":"the-one-architectural-difference-from-desktop"},{"t":"1. Install","a":"1-install"},{"t":"2. The four bootstrap shims","a":"2-the-four-bootstrap-shims"},{"t":"Path A — the WebView render path (what most apps want)","a":"path-a--the-webview-render-path-what-most-apps-want"},{"t":"Path B — the native-UI render path","a":"path-b--the-native-ui-render-path"},{"t":"Building and running","a":"building-and-running"},{"t":"Configuration","a":"configuration"},{"t":"Commands","a":"commands"}],"x":"Getting started — mobile Two things to know before you write any code. Nothing here has run on a phone. Every claim in the mobile bundle is backed by tests — including tests that byte-compare element trees against upstream's own collector and parse upstream's Kotlin and Swift to check coverage — but no build has been produced by Xcode or Gradle in this project, and no screen has been rendered on a device. The desktop side is backed by a screenshot of a running packaged app; mobile is not held to that standard yet. Treat it as a well-tested design awaiting device verification. Check the licensing yourself. NativePHP Mobile is sold as a product, but the repository these host projects come from carries an MIT (Bifrost Technology, LLC) and declares in its — so read the terms rather than trusting a summary, this one included. prints the same caution deliberately. The desktop runtime is MIT. Can you build an Android or iOS app today? Yes — on a machine with the platform toolchain. Nothing in this bundle is a stub, and no step is waiting on unwritten code. What it needs from you is the half no PHP package can provide: You need Why --- --- A clone of , and whatever its terms require of you The iOS and Android host projects live there. copies and retargets them; it cannot fetch them for you. Android Studio / the Android SDK, or Xcode produces the APK/AAB or the iOS archive by invoking or . Neither exists in a PHP container, and neither runs in CI without a licence. A keystore or a provisioning profile Signing is deliberately not handled here — see below. Everything on this side of that line runs and is covered by tests: staging, cleaning, the app archive, , the native-route manifest, toolchain detection, and the exact command plan. stops precisely at the boundary; prints the toolchain commands for you to run on a machine that has them. The honest caveat is verification, not capability: no build produced by these commands has been opened by Xcode or Gradle, and no screen has "},{"t":"Desktop API","u":"desktop-api.html","b":"Windows, menus, dialogs, processes, events","h":[{"t":"Three things that apply everywhere","a":"three-things-that-apply-everywhere"},{"t":"Windows","a":"windows"},{"t":"Menus","a":"menus"},{"t":"Dialogs","a":"dialogs"},{"t":"Notifications","a":"notifications"},{"t":"Clipboard","a":"clipboard"},{"t":"Settings","a":"settings"},{"t":"Child processes and Messenger workers","a":"child-processes-and-messenger-workers"},{"t":"Events","a":"events"},{"t":"The rest, briefly","a":"the-rest-briefly"},{"t":"Commands","a":"commands"}],"x":"Desktop API reference Every service below is registered and autowirable by class name. There are no facades and no static helpers: inject , , and so on. If you came from the Laravel version, that is the largest day-to-day difference — becomes an injected . This page shows the shape of each area and flags what behaves surprisingly. For the exhaustive list of all 116 endpoints with request and response shapes, read ; it is the specification, this is the guide. Three things that apply everywhere A dispatch is not an outcome. Most endpoints answer with the body , and several — every endpoint and both ones — send that before doing any work. A successful call means the runtime received your request. Nothing more. Unknown ids are silently ignored. The runtime uses optional chaining on its window map, so is a no-op, not an error. Outside the runtime, every call throws. is false when is unset, and any call then throws . Code that also runs as an ordinary web request must guard: covers the other two failures: a transport error, and a (the shared secret did not match). --- Windows — 21 endpoints. covers all 39 keys — geometry, , , , , , , , , , , , , . Read the class; it is a flat list of one-line setters. Reads: is a readonly value object with 21 typed properties ( , , , , , , , , , …). Surprises worth internalising. - is idempotent by id. An existing id is shown and focused and nothing is created. This is why calling it from is safe even though fires again on macOS . - is fallible by design. The runtime calls with no null guard, so when the app is backgrounded it throws on its side and answers 500. You get , never an exception. Never write . - A programmatic resize emits no . The runtime listens for Electron's , which fires for user drags but not . succeeds and reflects the new size; no event arrives. - is effectively required, and the bundle sends it for you. The runtime does on with no guard, so an absent value is and renders the page at an absurd zoom. defaults it to . - "},{"t":"Mobile API","u":"mobile-api.html","b":"54 bridge methods and the native-UI layers","h":[{"t":"The bridge","a":"the-bridge"},{"t":"The groups","a":"the-groups"},{"t":"Testing the bridge","a":"testing-the-bridge"},{"t":"Native UI","a":"native-ui"},{"t":"Commands","a":"commands"}],"x":"Mobile API reference All classes live under . The sixteen API services are registered and autowirable; they are thin, typed wrappers over . For the native-UI wire format — node shapes, hashes, callback ids, style vocabulary — read . This page is the guide to using it. --- The bridge has four methods, and the distinction between two of them is the most important thing on this page: Method Returns Use --- --- --- — decoded JSON, on any failure Reads, and writes whose result is known immediately — was the call made , not what it achieved Anything that presents UI The undecoded reply False outside a packaged app; every call then no-ops A dispatch is not an outcome. Camera, biometrics, media pickers and the payment sheet all return as soon as the UI is presented . The result arrives later as an event on the native side. Those methods deliberately sit on so the distinction is visible in the type. returning means a prompt appeared — treating it as \"the user authenticated\" is an authentication bypass. Non-JSON replies and throws from the extension are logged (channel ) and turned into , never swallowed silently, on the same principle as the desktop client: \"the native layer did not answer\" must not look like \"the native layer said nothing\". Where do the results arrive? On the WebView path, the host's JS bridge exposes — the same shape as desktop's preload — so an asynchronous result reaches the page , not PHP. This bundle implements no PHP-side receiver for mobile events ; there is no mobile equivalent of the desktop and no mobile event classes. If PHP needs to know about a captured photo, the page has to tell it (a to one of your routes). Plan for that when you design a flow around , , or . --- The groups Camera — All three are dispatches. Nothing is returned but \"the UI was presented\". Device — Secure storage — iOS Keychain and Android Keystore. Values survive app updates but not a reinstall or a move to another device — suitable for tokens, not for anything a user would "},{"t":"Wire protocol","u":"contract.html","b":"All 116 endpoints and 44 events, exactly","h":[{"t":"0. Transport and conventions","a":"0-transport-and-conventions"},{"t":"1. Window — 21 endpoints","a":"1-window--21-endpoints"},{"t":"2. App — 19 endpoints","a":"2-app--19-endpoints"},{"t":"3. System — 10 endpoints","a":"3-system--10-endpoints"},{"t":"4. Menu bar / tray — 9 endpoints","a":"4-menu-bar--tray--9-endpoints"},{"t":"5. Menu, dock, context menu — 11 endpoints","a":"5-menu-dock-context-menu--11-endpoints"},{"t":"6. Dialogs and alerts — 4 endpoints","a":"6-dialogs-and-alerts--4-endpoints"},{"t":"7. Clipboard — 7 endpoints","a":"7-clipboard--7-endpoints"},{"t":"8. Shell, screen, settings, process — 13 endpoints","a":"8-shell-screen-settings-process--13-endpoints"},{"t":"8b. Global shortcuts — 3 endpoints","a":"8b-global-shortcuts--3-endpoints"},{"t":"9. Notifications — 1 endpoint","a":"9-notifications--1-endpoint"},{"t":"10. Power monitor — 4 endpoints","a":"10-power-monitor--4-endpoints"},{"t":"11. Auto-updater and child processes — 11 endpoints","a":"11-auto-updater-and-child-processes--11-endpoints"},{"t":"12. Reverse channel — 44 events","a":"12-reverse-channel--44-events"},{"t":"13. Environment contract","a":"13-environment-contract"},{"t":"14. What the runtime requires of the app","a":"14-what-the-runtime-requires-of-the-app"},{"t":"15. Coverage","a":"15-coverage"}],"x":"NativePHP Desktop — Runtime Contract The wire protocol between a PHP application and the NativePHP Electron runtime. Extracted from @ HEAD (2026-08-14), . Framework-neutral by construction: nothing below mentions Laravel except where the current implementation leaks it, and those leaks are called out as such. This is the spec a adapter implements, and the spec a package would encode. See for the boot sequence and for the roadmap. --- 0. Transport and conventions Channel A — app → runtime - Base URL — supplied to the PHP process as (already includes the trailing ). - Port first free port in , chosen at runtime boot. - Auth on every request, from . Mismatch → with an empty body, before any routing ( ). - Body JSON ( ). Send . - Timeout the reference client uses 3600s. Several endpoints block on native UI ( , , ) and will not return until the user acts. Do not set a short timeout. - is mounted only when ( ). In production those requests 404. Treat as fire-and-forget. Response conventions Three shapes, and they are not consistent — a port must handle all three: 1. whose body is the status phrase , not JSON and not empty — most mutations. makes express send ; a from arrives as . Treat a body equal to the reason phrase for its status code as \"no data\" — and only that, so a genuine HTML error page is still visible as a problem. (This entry originally read \"no body\"; corrected after a client logged a JSON parse error on every successful mutation.) 2. with a JSON object — all reads, plus a few mutations. 3. sent before the work happens. Every endpoint and both endpoints call as their first statement, then act. A from those is an acknowledgement of receipt, not of success — errors there are unobservable by the caller. Error responses that do exist: ( ) from the encrypt/decrypt, print-to-pdf, and clipboard-image paths; from ; from and when the alias is unknown; from and . Channel B — runtime → app - , same header. Only those two endpoints exist. - Failures are silently swallow"},{"t":"Native-UI format","u":"native-ui-contract.html","b":"What SwiftUI and Compose consume","h":[{"t":"1. Transport","a":"1-transport"},{"t":"2. The node","a":"2-the-node"},{"t":"3. Node identity","a":"3-node-identity"},{"t":"4. Diffing — Merkle hashes and reuse markers","a":"4-diffing--merkle-hashes-and-reuse-markers"},{"t":"4b. ⚠ Id derivation — FNV-1a, and two different masks","a":"4b-⚠-id-derivation--fnv-1a-and-two-different-masks"},{"t":"4c. ⚠ Layout defaults, and two naming conventions","a":"4c-⚠-layout-defaults-and-two-naming-conventions"},{"t":"4d. The styling pipeline","a":"4d-the-styling-pipeline"},{"t":"6. Element types","a":"6-element-types"},{"t":"7. What M6 would actually involve","a":"7-what-m6-would-actually-involve"},{"t":"7b. Native routing and the manifest","a":"7b-native-routing-and-the-manifest"},{"t":"8. Caveats","a":"8-caveats"}],"x":"NativePHP Mobile — the native-UI wire format The protocol between PHP and the SwiftUI / Jetpack Compose renderers, extracted from . This is what a Twig front end would have to produce — the foundation for M6, the equivalent. Verified, not just read. now contains a second implementation of this format, and byte-compares its output against upstream's own collector — trees, content hashes, callback ids and navigation keys. Upstream's classes have no framework dependencies (their only mentions of Blade are in comments), so they load through a stub autoloader and run standalone. Three of the details below were wrong in the first draft of this document and the comparison test is what caught them; they are marked ⚠. The headline: the format is framework-neutral. It is a JSON tree with content hashes. Blade appears nowhere in it. The 17,316 LOC of is the authoring layer — a Blade tag precompiler, a Tailwind-subset parser, element classes, a component lifecycle — sitting on top of a protocol that has no opinion about how the tree was built. That is the difference between \"port a rendering engine\" and \"write a second front end for a documented protocol\", and it is why M6 is a real project rather than an impossible one. --- 1. Transport Not . The element tree has its own extension functions: Function When --- --- once, when the native runloop starts before building each frame once per frame, with the root node when the runloop stops From . So a frame is: reset → build → publish. Interactions arrive from the native side carrying a callback id, which PHP resolves, runs, and then publishes a new frame. is used alongside this to set the screen transition — the one bridge method that belongs to this path rather than the WebView one. --- 2. The node From . Every key except , and is omitted when empty, which keeps frames small: , and are deliberately separate. A renderer applies layout to its Yoga node, style to its view, and props to the widget — three different consumers, so mixing"},{"t":"Recipes","u":"recipes.html","b":"Multi-window, workers, native screens, packaging","h":[{"t":"A multi-window app, and the _windowId convention","a":"a-multi-window-app-and-the-_windowid-convention"},{"t":"Running a Messenger worker","a":"running-a-messenger-worker"},{"t":"Reacting to a runtime event with a typed listener","a":"reacting-to-a-runtime-event-with-a-typed-listener"},{"t":"A native-UI screen with state","a":"a-native-ui-screen-with-state"},{"t":"Building for distribution","a":"building-for-distribution"}],"x":"Recipes Complete, working answers to the things people actually build. Every sample here is written against the real classes; check the signatures in / if you extend them. - A multi-window app, and the convention - Running a Messenger worker - Reacting to a runtime event with a typed listener - A native-UI screen with state - Building for distribution --- A multi-window app, and the convention The runtime appends to every navigation it performs. That parameter is how a request knows which window it came from — and it is the whole mechanism, so it is worth understanding before you rely on it. Opening a second window from a controller, and closing it from inside itself: Three rules that follow from the mechanism: 1. Keep the fallback order. reads the Referer first, then the current URL, then gives up ( then defaults to ). A form POST carries the window in its Referer, not its own URI. \"Improving\" this order makes multi-window apps target the wrong window. 2. Make these responses . is part of the URL for HTTP cache purposes, and without it two windows can share a cached page. 3. Do not open windows outside a request unless you pass an absolute URL or set . builds the base from the current request because the dev server's port is chosen by the runtime and never published to PHP; outside a request it throws a that says exactly this. Reading the window back: Running a Messenger worker Watch it, and stop it: What does that you would otherwise have to remember: so the runtime's watchdog restarts a worker that exited on ; so shutdown is a plain SIGTERM to that process rather than a tree-kill that could drop the message in flight; and an ini of twice , because otherwise PHP fatals before Messenger notices its own limit and exits cleanly. The alias on the wire is — namespaced so a worker cannot collide with your own process aliases. Use the un-prefixed name in every call; use the prefixed one when reading or matching events. Worker output arrives as events: For a one-off job i"},{"t":"Testing","u":"testing.html","b":"FakeRuntime, expectations, event simulation","h":[{"t":"Wiring: one flag and one trait","a":"wiring-one-flag-and-one-trait"},{"t":"Faking the desktop runtime","a":"faking-the-desktop-runtime"},{"t":"Asserting what the app asked for","a":"asserting-what-the-app-asked-for"},{"t":"Pushing events into the app","a":"pushing-events-into-the-app"},{"t":"Mobile","a":"mobile"},{"t":"What these tests cannot tell you","a":"what-these-tests-cannot-tell-you"}],"x":"Testing Desktop apps are testable without Electron, and mobile apps without a device. Both bundles put the double at the transport seam rather than in front of each manager, which is the difference between testing your payloads and testing a stub. Everything here needs , which your app already has wherever you would use it. - Wiring: one flag and one trait - Faking the desktop runtime - Asserting what the app asked for - Pushing events into the app - Mobile - What these tests cannot tell you --- Wiring: one flag and one trait That re-points the alias at and nothing else : every manager, every controller and the event bridge stay exactly as they are in production, so a functional test exercises the real payload building against a runtime that records instead of one that has to be running. It also registers , and when PHPUnit is present — all three public. Set it in only. With it on, nothing reaches a real runtime. The trait wires the three pieces in one , and works both ways round: - In a with , finds the fake the container already injected everywhere — so the services under test are asserted against the same call log without the test handing any of them a double. Two fakes in one test, with assertions made against the one nobody called, is the failure mode this avoids. - In a plain it builds its own , and you construct managers with . is provided because needs a and a ; every other manager is just . The trait's surface: --- --- The fake every native call goes through Assertions Push events; defaults to the container's dispatcher so the app's real listeners run A dispatcher owned by the test, for a listener in isolation Pretend this request came from that window matters more than it looks: the runtime appends to everything it navigates, and every manager method with an optional id falls back to it. Without a request carrying the parameter, an id-less call resolves to and the test cannot distinguish a correct fallback from a lost id. Faking the desktop runtime impleme"},{"t":"Troubleshooting","u":"troubleshooting.html","b":"Symptom → cause → fix","h":[{"t":"Desktop — boot","a":"desktop--boot"},{"t":"Desktop — windows and events","a":"desktop--windows-and-events"},{"t":"Desktop — packaging","a":"desktop--packaging"},{"t":"Desktop — tooling","a":"desktop--tooling"},{"t":"Mobile","a":"mobile"}],"x":"Troubleshooting The dominant failure mode of this runtime is silence . It carries on with an empty config when fails, and it ignores unknown window ids. So most entries below are symptom-first, because the symptom is usually all you get. The worst of it — swallowing every failed callback, which made a crashed, 500ing or 403ing app indistinguishable from a healthy one — is fixed in the copy of the runtime this bundle installs . carries that fix and four others; see what the bundle patches. So under a failed callback now says so. Two things to do before reading further: and watch your own log ( ). The bundle logs deliberately where the runtime would swallow: a missing bootstrapper, a non-JSON response, a failed broadcast. - Desktop — boot - Desktop — windows and events - Desktop — packaging - Desktop — tooling - Mobile --- Desktop — boot My app boots to a blank window, or to no window at all Five distinct causes. checks for the first — and the second is handled for you by default: The routes are not imported. Symfony bundles cannot register routes, so the runtime's 404s and nothing ever asks for a window. If they are missing, add the import shown in getting started. Your firewall covers the runtime's own endpoints. The bundle handles this for you by default — but it is worth knowing about, because the symptom is indistinguishable from the one above and the cause is in a file you wrote. runs at priority 4096 and returns without stopping propagation — it cannot do otherwise, since the router listens at 32 and your app may have listeners of its own below the firewall. So the firewall still evaluates, and an rule of — which is what most authenticated apps have — answers the runtime's with a 401 that the runtime discards. Laravel is not exposed to this: there the two routes are registered outside the app's middleware groups entirely. (on by default) decorates Symfony's so those paths carry no attributes while running inside the runtime . Outside it — the same codebase depl"},{"t":"Architecture","u":"architecture.html","b":"Desktop vs mobile, and the shared traps","h":[{"t":"The one-line difference","a":"the-one-line-difference"},{"t":"What the two bundles share, and what they don't","a":"what-the-two-bundles-share-and-what-they-dont"},{"t":"The three verification techniques","a":"the-three-verification-techniques"},{"t":"Where the bodies are buried","a":"where-the-bodies-are-buried"},{"t":"Reading order","a":"reading-order"}],"x":"Architecture — how the two halves fit together Desktop and mobile are separate products upstream that happen to share a brand. This project ports both, and the shapes are genuinely different — worth seeing side by side before reading either bundle. --- The one-line difference Desktop is two processes talking over a socket. Mobile is one process with PHP compiled into it. Everything else follows from that. desktop mobile --- --- --- Transport localhost HTTP + JSON PHP extension functions Auth on every call none needed — same process Surface 116 endpoints, 44 pushed events 54 bridge methods PHP lifecycle , one process per request compiled in; one-shot or persistent Needs a SAPI shim no ( + a router script) yes — the host reads stdout as HTTP UI WebView, always WebView or native element tree Framework leak in the native layer 8 string literals, 1 file 3 bootstrap paths, 2 files Verified by a running packaged app tests only — no Xcode, no Android SDK here That last row is the one to keep in mind. The two halves are not held to the same standard of evidence, and this project says so wherever mobile is described rather than letting the reader assume parity. --- What the two bundles share, and what they don't Almost nothing, and that is the right answer rather than a missed opportunity. Not shared, because the mechanisms genuinely differ: the transport (HTTP client vs extension function), the security model (a shared secret vs same-address-space), the event model (44 pushed classes vs 54 synchronous or event-returning calls), the build pipeline (electron-builder vs Gradle and Xcode). Genuinely common, and the natural content of a future : - — \"the runtime is up, decide what appears\". Identical on both. - The dispatch discipline: a call that presents UI is separated from one that returns a result, so a can never be mistaken for an outcome. - The diagnostic discipline: never let \"the runtime did not answer\" look like \"the runtime said nothing\". Both bundles log rather than s"},{"t":"Deep dive","u":"analysis.html","b":"Boot sequence, port map, the Laravel-isms","h":[{"t":"1. The boot sequence, exactly","a":"1-the-boot-sequence-exactly"},{"t":"2. The two channels and the auth model","a":"2-the-two-channels-and-the-auth-model"},{"t":"3. What the runtime requires of the PHP app — the real contract","a":"3-what-the-runtime-requires-of-the-php-app--the-real-contract"},{"t":"4. Reverse channel — the 44 events","a":"4-reverse-channel--the-44-events"},{"t":"5. Port map — where the 9,615 LOC go","a":"5-port-map--where-the-9615-loc-go"},{"t":"6. The six Laravel-isms in the Electron plugin, with locations","a":"6-the-six-laravel-isms-in-the-electron-plugin-with-locations"},{"t":"7. Symfony-specific design problems and resolutions","a":"7-symfony-specific-design-problems-and-resolutions"},{"t":"8. Findings worth reporting upstream","a":"8-findings-worth-reporting-upstream"},{"t":"9. Open questions that need maintainer input","a":"9-open-questions-that-need-maintainer-input"},{"t":"10. Mobile — a correction","a":"10-mobile--a-correction"},{"t":"11. Revised milestones","a":"11-revised-milestones"}],"x":"NativePHP Desktop — Deep Technical Analysis Reference document for porting NativePHP to Symfony. Read alongside . Based on a full read of @ HEAD (2026-08-14): 9,615 LOC PHP (178 files) + 4,100 LOC TypeScript, and @ HEAD: 54,744 LOC PHP (272 files). Everything below is read out of the source, not from the docs. --- 1. The boot sequence, exactly This is the single most important thing to understand. From : Step 12 is the whole app-startup contract. The runtime never opens a window. It calls one endpoint and the app decides. That is a two-line port. Note the ordering constraint at step 9b: and are only in the env because the API server started at step 7. The PHP process receives them as environment variables — there is no discovery, no handshake, no config file. reads and the uses it as a base URL. Trivially portable to . --- 2. The two channels and the auth model Channel A — PHP → runtime (116 endpoints) - Base URL: ( ) - Auth: on every request. is nine lines: wrong header → , full stop. No CSRF, no session, no bearer. - Content type: JSON both ways. Many endpoints return bare with no body. - Client: , 38 LOC, three methods ( / / ), . Channel B — runtime → PHP (44 event types) - → with the same header. Two endpoints only: and . - Errors are swallowed ( with an empty body). A crashed PHP app is invisible to the runtime. Worth knowing when debugging a port. - also forwards to every renderer via , so browser JS sees the event without a PHP round-trip. Channel C — runtime → renderer (the browser) - exposes and via , plus a bridge and a CustomEvent on the window. - is entirely framework-agnostic. It works in a Twig page as-is. - Only is Laravel-specific — a 30-line shim that turns postMessage into . It is injected into every full-page HTML response by hooking Laravel's event. A Symfony port replaces this file with a Stimulus/Turbo (or plain ) dispatcher and injects it from a subscriber. Three-layer security model 1. cookie, set by Electron directly into the session ( ) — v"},{"t":"Mobile analysis","u":"mobile-analysis.html","b":"Both render paths, and a corrected conclusion","h":[{"t":"1. There are two render paths, and one of them is a WebView","a":"1-there-are-two-render-paths-and-one-of-them-is-a-webview"},{"t":"2. What mobile actually requires of the PHP app","a":"2-what-mobile-actually-requires-of-the-php-app"},{"t":"3. The same escape hatch exists","a":"3-the-same-escape-hatch-exists"},{"t":"4. Revised difficulty","a":"4-revised-difficulty"},{"t":"5. Plan","a":"5-plan"},{"t":"6. Still open","a":"6-still-open"}],"x":"NativePHP Mobile — analysis, and a correction My earlier verdict was wrong. §2.3 and §10 called mobile a different order of magnitude, on the grounds that its UI is a 17,316-LOC native rendering engine coupled to Blade. The engine is real and that number is right. What I got wrong is that it is optional . I measured the hard path and treated it as the only path. Reading the actual boot code rather than the file listing shows there are two, and the easy one is the one desktop already uses. --- 1. There are two render paths, and one of them is a WebView — its own docblock: Decides how the first screen boots: direct JNI dispatch into the native runloop (no WebView, no Chromium) or the legacy WebView path. […] Missing or unparseable manifest ⇒ WEB LEGACY — always safe, byte-identical to the pre-native-first behavior. The decision is data-driven from a native-route manifest that the CLI bakes into , listing the URI patterns. The logic: is the same design, / , same check. And the escape hatch is a documented, first-class env var. From and : Consequences for a Symfony port. A Symfony app has no patterns, so it produces no manifest, so every path falls through to — and makes that explicit rather than incidental. The Edge engine is never entered. Twig renders into a WebView exactly as it does on desktop, and zero of those 17,316 lines need porting for a working mobile app. The WebView path is not a deprecated corner either: it has a full supporting stack ( at 655 lines, , , , ) and its JS bridge exposes — the same shape as desktop's preload. --- 2. What mobile actually requires of the PHP app Different from desktop in one important way: there is no HTTP boundary . PHP is compiled into the app and called through JNI (Android) or directly (iOS), so there is no port, no secret, no express server. Two mechanisms instead: 2a. A hand-rolled SAPI The native layer executes a bootstrap script by absolute path and reads its stdout as a raw HTTP response. From : and : reconstructs / / "},{"t":"Plan","u":"plan.html","b":"The milestones, and where each one landed","h":[{"t":"1. What NativePHP actually is (as of Aug 2026)","a":"1-what-nativephp-actually-is-as-of-aug-2026"},{"t":"2. Coupling audit — where Laravel actually leaks","a":"2-coupling-audit--where-laravel-actually-leaks"},{"t":"3. Decision: start with desktop. Do not touch mobile yet.","a":"3-decision-start-with-desktop-do-not-touch-mobile-yet"},{"t":"4. Plan","a":"4-plan"},{"t":"5. Political / process notes","a":"5-political--process-notes"},{"t":"6. Reference — local checkouts used for this study","a":"6-reference--local-checkouts-used-for-this-study"}],"x":"NativePHP for Symfony — Architecture Study & Implementation Plan Status: planning. Written 2026-08-14 after a full read of the NativePHP org. Prior art: NativePHP/laravel discussion 504 (opened by @sadiqk2, Feb 2025). See for the deep technical reference : the exact boot sequence, the full runtime contract, the 44-event reverse channel, a per-directory port map with LOC, the eight Laravel-isms in with line numbers, Symfony-specific design resolutions, eight upstream bugs worth reporting, and the M1 recipe. is the wire protocol (all 116 endpoints, 44 events, env vars). is the working proof, with the findings that only running it revealed. covers the bundle, the build pipeline, and is the package's own documentation. covers mobile — and corrects §2.3 and §10's verdict on it. --- 1. What NativePHP actually is (as of Aug 2026) It is two unrelated products that share a brand, not one framework with two targets. 1.1 (repo , ★422) The current desktop monorepo. It absorbed three now- archived repos: (★3.9k), (★516), (★54). Anything you read about or is historical — do not build against it. Architecture — two OS processes : The boundary is already framework-agnostic. It is localhost HTTP + JSON. Nothing in the Electron process knows what a Laravel is except in a handful of specific places (see §2.2). That is the whole opportunity. Endpoint surface, from — 116 endpoints across 22 modules: module eps module eps module eps --- --- --- --- --- --- window 21 clipboard 7 dialog 2 app 19 shell 4 contextMenu 2 system 10 settings 4 alert 2 menuBar 9 screen 4 progressBar 1 dock 8 powerMonitor 4 process 1 childProcess 8 globalShortcut 3 notification 1 autoUpdater 3 menu / debug / broadcasting 1 each 1.2 (repo , ★1134) Completely different. PHP is compiled into the iOS/Android app bundle: - — PHP embedded via NDK, - — hand-rolled SAPI: builds from , , , echoes a raw HTTP response - + — long-lived interpreter, requests dispatched via into - Native calls go through a PHP extension functio"},{"t":"Desktop bundle","u":"desktop-bundle.html","b":"What ships in native-symfony/desktop-bundle","h":[{"t":"What it gives you","a":"what-it-gives-you"},{"t":"Install","a":"install"},{"t":"Configuration","a":"configuration"},{"t":"Commands","a":"commands"},{"t":"Why patch the runtime?","a":"why-patch-the-runtime"},{"t":"Building","a":"building"},{"t":"Security","a":"security"},{"t":"Two behaviours worth knowing","a":"two-behaviours-worth-knowing"},{"t":"Tests","a":"tests"}],"x":"native-symfony/desktop-bundle Build desktop applications with Symfony, on NativePHP's Electron runtime. Status: M3 complete. All 116 runtime endpoints and all 44 events are implemented, and produces a distributable app that has been built and run — see . 392 tests. Not done yet: installer targets beyond , and code signing (the env plumbing is there, untested without real credentials). The vendor name is provisional — is someone else's brand, and asking comes before claiming it. What it gives you Autowired services, no facades: , , , , . Install patches the runtime, and skips it automatically when it detects one that already reads a — so once the upstream manifest change lands, is the whole story and nothing is patched. Then implement on any service. The bundle autoconfigures and aliases it — no wiring needed. Routes cannot be auto-registered (Symfony bundles cannot register their own), so writes the import for you: It only ever creates the file, never rewrites an edited one without , and warns instead of guessing if there is no . Without this import the app boots into a window that shows nothing, because 404s and is never called. Configuration Commands Command Purpose --- --- Start the app in development Copy, patch and build the Electron runtime Print the startup config as JSON — the runtime calls this Print PHP ini overrides as JSON — the runtime calls this Package for distribution; skips installer generation Once-a-minute tick the runtime drives; no-op by default The two marked commands are part of the wire contract. They print nothing but JSON on stdout, and they run before the runtime's API server exists — so they must never assume is set. Why patch the runtime? The runtime hardcodes eight Laravel-specific values as string literals in its TypeScript: the CLI name (six call sites), the router script path ( ), a directory it copies unconditionally, , and . rewrites them. This is not a fork. upstream already mirrors the whole Electron project into the application'"},{"t":"Mobile bundle","u":"mobile-bundle.html","b":"What ships in native-symfony/mobile-bundle","h":[{"t":"Why there is no Edge port here","a":"why-there-is-no-edge-port-here"},{"t":"What it gives you","a":"what-it-gives-you"},{"t":"Install","a":"install"},{"t":"Configuration","a":"configuration"},{"t":"Testing without a device","a":"testing-without-a-device"},{"t":"The two things most likely to bite","a":"the-two-things-most-likely-to-bite"},{"t":"Native UI from Twig","a":"native-ui-from-twig"},{"t":"Architecture","a":"architecture"},{"t":"Tests","a":"tests"}],"x":"native-symfony/mobile-bundle Build iOS and Android applications with Symfony, on NativePHP's mobile runtime. Status: the WebView render path is implemented, and the native-UI wire format is implemented and verified. All 54 native bridge methods are wrapped, the SAPI shim and persistent runtime are in place, element trees are byte-identical to upstream's, and 377 tests cover it. Verified by tests, not by a device. Unlike the desktop bundle — whose claim is backed by a screenshot of a running packaged app — nothing here has run on a phone or an emulator. This environment has no Xcode and no Android SDK. Treat mobile as a well-tested design awaiting device verification, and read that distinction as load bearing. Licence. NativePHP Mobile is a commercial product, unlike the MIT-licensed desktop runtime. Read its terms before distributing anything built this way. Why there is no Edge port here Mobile has two render paths. The native-UI one draws SwiftUI and Jetpack Compose from Blade templates, and its engine is 17,316 LOC — the reason an earlier version of this project wrote mobile off entirely. It is optional. , on both platforms, falls back to a WebView whenever an app registers no patterns, and forces it at build time. Its own docblock calls that path \"always safe, byte-identical to the pre-native-first behavior\" . A Symfony app registers no native routes, so it takes the WebView path by construction — and Twig in a WebView is just Twig. Full reasoning: . What it gives you Sixteen autowired API services: , , , , , , , , , , , , , , , . Install Then build with Android Studio or Xcode. There is no headless path — both toolchains are required. Configuration Testing without a device is a compiled extension that exists only inside a packaged app, so outside one the bridge is always unavailable and every call returns null. Two ways around that, both shipped rather than kept in this package's own tests: The real also accepts an callable standing in for the extension functio"},{"t":"The demo app","u":"demo.html","b":"Deskpad: both bundles, driven end to end","h":[{"t":"What to look at","a":"what-to-look-at"},{"t":"Running it","a":"running-it"},{"t":"What was actually verified","a":"what-was-actually-verified"},{"t":"What was not verified, and cannot be here","a":"what-was-not-verified-and-cannot-be-here"},{"t":"How this differs from spike/app","a":"how-this-differs-from-spikeapp"},{"t":"What building this changed in the bundles","a":"what-building-this-changed-in-the-bundles"},{"t":"Layout","a":"layout"}],"x":"Deskpad — the demo application A small notes app, built with Symfony 8, running in a native window on NativePHP's Electron runtime. It installs both bundles — and — and it doubles as their integration test: everything below was captured by booting the real runtime headlessly and driving it. Two windows, one PHP process. The application menu at the top is built in PHP. The event feed along the bottom is . The count in the second window went from 0 to 1 without a reload, because a PHP event was broadcast to every renderer. What to look at Read these five files and you have seen the whole desktop surface. File Why --- --- The entire app-startup contract: the runtime POSTs , this decides what appears. Opens the main window, builds the application and context menus, registers a global shortcut. What menu items do . An item carries a name, not a callback — clicking one comes back as an event over HTTP, and this is the other end of it. The reverse channel, typed: , not . Ten of the runtime's 44 events, with the two behaviours that surprise people written down where they bite. The app's one real feature. Saving a note touches the settings store, a notification and a broadcast; exporting one opens a native save dialog. An ordinary Symfony event that also reaches every window's JavaScript, because it implements . And for mobile, three: File Why --- --- A component: state as a typed property, one the device may call, one public method it may not. A native screen authored with the and Twig functions — no HTML, no WebView. The page prints the wire tree it produced. Why native screens are listed explicitly instead of scanned for: the build-time list and the runtime list have to be the same list. The screens Dashboard ( ) — every value is a live round trip to the Electron process over localhost HTTP. Window geometry, the detected , the Electron process, the system theme, the display, the userData path. Opens and closes the second window. Notes ( ) — create, copy and delete notes p"},{"t":"Upstream patches","u":"upstream-patches.html","b":"Eleven fixes, ten of them open PRs","h":[{"t":"The one that matters","a":"the-one-that-matters"},{"t":"The independent fixes","a":"the-independent-fixes"},{"t":"0008 — a bug in the mobile Tailwind parser","a":"0008--a-bug-in-the-mobile-tailwind-parser"},{"t":"0009 — optional route segments were never resolvable in PHP","a":"0009--optional-route-segments-were-never-resolvable-in-php"},{"t":"0010 — the callback expression parser corrupts and drops arguments","a":"0010--the-callback-expression-parser-corrupts-and-drops-arguments"},{"t":"0011 — dark: buries a theme token's dark value","a":"0011--dark-buries-a-theme-tokens-dark-value"},{"t":"Suggested order","a":"suggested-order"},{"t":"What is deliberately not here","a":"what-is-deliberately-not-here"},{"t":"Provenance","a":"provenance"}],"x":"Upstream patches — ten submitted, one held Eleven patches. Ten are open pull requests ; the eleventh is held deliberately. repo base status --- --- --- --- – @ open PRs 136– 141 , awaiting review – @ open PRs 349– 352 , awaiting review @ held until the small ones land — it is the one that needs a maintainer conversation The order is deliberate: ten small, individually reviewable fixes first, so that the manifest patch — the only one that asks upstream to change a design — arrives to someone who has already merged code from the same author. The four each carry a regression test in the repo's own Pest idiom, written against its real , and rather than a stub. Every test was checked against unpatched first: 3 of 3, 4 of 6, 3 of 6 and 1 of 3 fail there, which is the only evidence that they test the fix rather than the code. The full upstream suite passes at 887 with the same 13 pre-existing failures has on its own (Android splash-screen and release-build cases, unrelated), and is clean. Writing those tests also found a dead branch in — a double quote inside a double-quoted literal is caught by the delimiter check above it, so the escape branch for that case was unreachable. Removed before submitting. Patch PR --- --- theme border must not clobber an explicit width 349 variant buries a theme token's dark value 350 optional route segments never resolved 351 expression parser corrupts and drops arguments 352 Each patch file here is a of the branch that was pushed, so it includes the tests and matches the pull request exactly. Every patch applies cleanly to a clean tree, individually and as a series — verified with , and re-verified against 's current before the four were submitted. typechecks clean under the project's own . – target the mobile repo. An earlier note here called it commercial rather than MIT: that was wrong about the repository, which carries an MIT (Bifrost Technology, LLC) and declares in its . What is commercial is NativePHP Mobile as a product — the licen"},{"t":"M1 — the spike","u":"spike-results.html","b":"Proving it possible at all","h":[{"t":"What was proven","a":"what-was-proven"},{"t":"The diff against the runtime","a":"the-diff-against-the-runtime"},{"t":"New findings — things only running it revealed","a":"new-findings--things-only-running-it-revealed"},{"t":"Notes on the harness","a":"notes-on-the-harness"},{"t":"What this changes about the plan","a":"what-this-changes-about-the-plan"}],"x":"M1 Spike — Symfony 8 running under the NativePHP desktop runtime Result: it works. A Symfony 8.1.4 app on PHP 8.4.24, in a native Electron window, driving the runtime's API and receiving its events. No Laravel anywhere in the app. Reproduce with . Screenshots: (first success, with devtools), (final, after a programmatic resize). --- What was proven Channel Proof --- --- Boot handshake The runtime POSTed ; the Symfony controller answered by calling . The window in the screenshot exists only because Symfony asked for it. A — app → runtime (write) created the window at exactly the requested 1000×720 with the requested title. Then → → . A — app → runtime (read) read back into Twig: id , size, position, title, focused. After the resize the same read returned 760 × 520 — verified numerically, not from pixels. B — runtime → app Symfony's log recorded and the same for . Positional payload spreading exercised. C — runtime → renderer The page printed and then . No Livewire, no bridge shim — the preload's is framework-agnostic and works in a plain Twig page as-is. Security model Request carrying → . Same request without it → 403 , from the ported . Env contract reached Symfony (the page's green badge). 's came back through the runtime and appeared in the spawned server's flags. Total app-side code: 7 PHP classes + 1 router script + 1 Twig template. No bundle, no abstraction layer, no . --- The diff against the runtime Five hunks, two files, all inside ( is the executable version): Hunk Change Why --- --- --- 1 → (6 sites) + the two guards CLI entrypoint 2 router script → Symfony has no built-in-server router script 3 guard with Symfony has no dir 4 in same as 1 5 / → / found by running it — see below No upstream changes were needed. mirrors the whole Electron project into the app's own , and prefers that copy whenever a exists there. So an adapter can ship a patched runtime today and treat the upstream manifest PR as hygiene rather than a blocker — as predicted in §6. --- New "},{"t":"M2 — the bundle","u":"m2-results.html","b":"The 116-endpoint surface","h":[{"t":"Phase 1 — what shipped","a":"phase-1--what-shipped"},{"t":"Proven end-to-end","a":"proven-end-to-end"},{"t":"Phase 1 findings — again, from running it","a":"phase-1-findings--again-from-running-it"},{"t":"Corrections applied to the earlier documents","a":"corrections-applied-to-the-earlier-documents"},{"t":"Phase 2 — the remaining 76 endpoints","a":"phase-2--the-remaining-76-endpoints"},{"t":"What is left","a":"what-is-left"}],"x":"M2 — the bundle A real Symfony bundle, , replacing the spike's inline classes. Phase 2 is complete: all 116 runtime endpoints and all 44 events are implemented , verified by a test that parses the runtime's own express routers. 222 tests, 400 assertions. 120 files, ~6,200 LOC of source and ~1,600 of tests. Phase 1 (below) delivered Window, App, the event bridge, the security model and the install/run tooling. Phase 2 (§\"Phase 2\" further down) added the remaining 76 endpoints. Screenshots: (phase 1), (phase 2, with the custom application menu). Bundle docs: . --- Phase 1 — what shipped Area Contents --- --- Contracts (future ) , , , , Transport on , with / Window — all 21 endpoints , a fluent covering all 39 keys, a typed , App — all 19 endpoints , enum, (the 13 env-supplied locations) Event bridge with the dual spread, 12 typed event classes, , , HTTP , , bundle-shipped routes Security at priority 4096 DI config tree, , autoconfiguration Tooling (copy + patch + npm + plugin rebuild), , , , , The runtime patch is now PHP ( ), replacing the spike's Python script: idempotent, seven hunks, and it throws rather than skip when a target has moved upstream. Proven end-to-end Same harness as M1, now against the bundle. Zero errors and zero criticals in the app log: The page additionally renders (Referer-based detection), ( ), ( ) and ( , from the environment, no round-trip). --- Phase 1 findings — again, from running it 1. sends the body , not an empty body §0 said \"200 with no body\". That is wrong: express's sets the body to the status phrase , so a successful mutation arrives as with a body of , and a missing window as with . My client logged a JSON parse error for every mutation until I noticed. The naive fix — \"treat any non-JSON body as no data\" — is too broad; it would also swallow a genuine HTML error page, which is the one thing worth reporting. The client now compares the body against the exact reason phrase for the status code, so status-phrase bodies are silent an"},{"t":"M3 — the build","u":"m3-results.html","b":"Packaging, and what it got wrong first","h":[{"t":"The pipeline","a":"the-pipeline"},{"t":"Proof: the packaged app runs","a":"proof-the-packaged-app-runs"},{"t":"Findings","a":"findings"},{"t":"What is left","a":"what-is-left"}],"x":"M3 — the build pipeline packages a Symfony application into a distributable desktop app, and the packaged app has been built and run , not just built. 235 tests. Screenshot: — that window is the packaged binary from , running the staged app under the static PHP from . --- The pipeline + mirror upstream's sequence, because the shape is dictated by electron-builder's entry: whatever sits at is copied into the package, so \"building\" means assembling that directory. 1. Pre-build hooks — shell commands from config, run in the project root. A failure stops the build before anything is staged. 2. Stage the app into , skipping excluded paths. The walk uses a , so an excluded directory is never descended into — the difference between a fast build and copying before deleting it. 3. in the build directory, then drop (tens of megabytes of binaries for every platform, only one of which is needed, and it lives outside ) and . 4. Clean the — strip secrets, force / . Only the staged copy is touched; the developer's own file is never modified, and there is a test for that. 5. CA bundle and icons — from (without it the packaged app has no working outbound TLS), icons into both the build dir and electron-builder's . 6. Patch — electron-builder reads the app's identity from there, not from our config. 7. electron-builder , whose hook runs to unzip the target platform's static PHP over whatever the dev machine left behind. 8. Post-build hooks. produces an unpacked directory instead of an installer, which skips needing , or an AppImage runtime — the fast path for checking a build actually works. is a clean dependency Worth stating because it was not obvious: has zero PHP dependencies . It is static binaries for linux/mac/win × x64/arm64 × PHP 8.3/8.4/8.5, plus , multi-licensed. So a Symfony app can require it without pulling in anything Laravel — unlike , which would drag in , and . --- Proof: the packaged app runs Built for linux-x64, then launched from under Xvfb: The window reports, f"}]